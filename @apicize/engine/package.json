{
  "name": "@apicize/engine",
  "version": "0.0.1",
  "description": "Store configuration and execute Apicize tests locally",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "watch": "tsc --watch",
    "rebuild": "rm -rf dist && rm ./tsconfig.tsbuildinfo && tsc"
  },
  "author": "",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "babel": {
    "presets": [
      "@babel/preset-typescript"
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "./test/setup.ts"
    ]
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/node": "^20.8.10",
    "reflect-metadata": "^0.1.13",
    "ts-node": "^10.9.1",
    "undici-types": "^5.27.2"
  },
  "dependencies": {
    "@apicize/definitions": "^0.0.1",
    "chai": "^4.3.10",
    "mime": "^4.0.0",
    "sqlite": "^5.0.1",
    "sqlite3": "^5.1.6"
  }
}
