{
  "name": "@apicize/definitions",
  "version": "0.0.1",
  "description": "Basic model and service definitions for Apicize",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "watch": "tsc --watch",
    "rebuild": "rm -rf dist && rm ./tsconfig.tsbuildinfo && tsc"
  },
  "author": "",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "babel": {
    "presets": [
      "@babel/preset-typescript"
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "./test/setup.ts"
    ]
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^20.8.10",
    "jest": "^29.6.2",
    "reflect-metadata": "^0.1.13",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "undici-types": "^5.27.2"
  }
}
